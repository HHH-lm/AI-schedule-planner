# E-T018-001 微信提醒 remindAt 保存链路修复验证证据

- 任务：T-018 修复微信提醒 remindAt 保存链路（F-018 修订）
- 证据 ID：E-T018-001
- 时间：2026-08-11T03:10:00Z
- 验证方法：Supabase 云端数据核查 + 前端测试 + lint + 类型检查 + 生产构建
- 验证命令与结果：
  - Supabase 核查：`schedule_state` 32 个时间块全部缺少 `remindAt`，`reminder_log` 为空，确认 `pushed:0` 由保存链路丢字段导致
  - `npm test`：13 个测试文件 60 个测试全部通过（新增 defaultRemindAtISO 1 个测试），退出状态 0
  - `npm run lint`：通过（`--max-warnings=0`），退出状态 0
  - `npx tsc --noEmit`：通过，退出状态 0
  - `npm run build`：通过（Next.js 15.5.23 Turbopack），退出状态 0

## 根因与修复

- 根因：`saveBlock` 构造 `TimeBlock` 时遗漏 `draft.remindAt`，BlockModal 无论怎么填写提醒，保存后都会被丢弃
- 修复：`saveBlock` 保留 `remindAt`；`addParsedBlocks`、`planTasks` 自动为已排期新块写入默认提醒；`WeekTimeline` 待排期拖拽落点补写默认提醒
- `src/lib/date.ts` 新增 `defaultRemindAtISO`，未设置提醒的时间块统一取开始前 5 分钟

## 已知限制

- 云端既有时间块不会自动补提醒，打开编辑保存一次后生效；如需批量迁移未来未完成块另出任务

- 证据位置：`.project-to-act/tasks/T-018/evidence/E-T018-001.md`
- 有效期：2026-08-18T03:10:00Z
