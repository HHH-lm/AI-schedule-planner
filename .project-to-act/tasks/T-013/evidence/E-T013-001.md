# E-T013-001 FastAPI/Python 后端重构验证证据

- 任务：T-013 FastAPI/Python 后端重构
- 证据 ID：E-T013-001
- 时间：2026-08-10T12:10:00Z
- 验证方法：后端 pytest + 前端自动测试 + lint + 类型检查 + 生产构建 + 密钥扫描 + dev 联动探测
- 验证命令与结果：
  - `cd backend && .venv/bin/python -m pytest -q`：22 个测试全部通过，退出状态 0
  - `npm test`：12 个测试文件 52 个测试全部通过，退出状态 0
  - `npm run lint`：通过（`--max-warnings=0`），退出状态 0
  - `npx tsc --noEmit`：通过，退出状态 0
  - `npm run build`：通过（Next.js 15.5.23 Turbopack），退出状态 0
  - `npm run scan:secrets`：5 项全部 PASS，退出状态 0
  - dev 联动：`GET /api/v1/health` 返回 `status: ok`；`POST /api/v1/parse` 经 Next.js 代理调用 DeepSeek 成功；`/api/v1/breakdown`、`/api/v1/plan`、`/api/v1/conflicts/check` 均返回预期结果
- 代码版本：v0.1.0；T-013 FastAPI 后端重构

## 变更说明

- 新增 `backend/` FastAPI 项目：`app/main.py` 聚合路由，`app/services/` 实现 NLP、AI 调用、冲突检测、任务拆解与时间规划，`tests/` 提供 pytest 覆盖
- Next.js 前端通过 `next.config.mjs` rewrites 将 `/api/v1/*` 代理到 `BACKEND_URL`（默认 `http://127.0.0.1:8000`）
- 前端移除本地 NLP、AI 调用与冲突检测实现，QuickAdd / TaskBoard / 冲突弹窗统一调用后端
- `scripts/dev.sh` 同时启动 FastAPI（8000）与 Next.js（3000），复用原有防多实例逻辑
- 生产构建切换到 Turbopack（`next build --turbopack`），规避 Next.js webpack 在含中文/空格路径下的 Client Manifest 构建错误，同时保留安全的 next 15.5.23 版本

## 已知限制

- FastAPI 后端与前端需同时部署；前端 `/api/v1/*` 依赖 `BACKEND_URL` 可达
- 真实 OpenAI/DeepSeek 调用已在本地验证（DeepSeek 返回成功），生产网络与余额仍需部署环境人工验收

- 证据位置：`.project-to-act/tasks/T-013/evidence/E-T013-001.md`
- 有效期：2026-08-17T12:10:00Z
