# E-T021-001 真实部署环境验收清单证据

- 任务：T-021 真实部署环境验收清单（Supabase Auth/RLS、真实 AI 调用、微信推送端到端）
- 证据 ID：E-T021-001
- 时间：2026-08-15T20:47:58Z
- 验证方法：生产模式 `next start` + uvicorn；真实 Supabase Auth/Admin API + PostgREST/RLS；真实 DeepSeek API；真实 PushPlus
- 验证结果：Auth/RLS 15 项、DeepSeek 解析 1 项、PushPlus 端到端 8 项全部 PASS，退出状态 0
- 版本：v0.1.0；工作树包含本次验收文档与证据（未提交），另有既有未跟踪文件 `agentops-health-check-2026-08-16.md`（非本次任务产物）

## 运行环境

- 前端：`npm run start`（Next.js 15.5.23 生产构建），`http://localhost:3000`
- 后端：`.venv/bin/python -m uvicorn app.main:app --port 8000`
- 健康检查：前端 `/api/health` 返回 `status: ok`、`storage: supabase`；后端 `/api/v1/health` 返回 `status: ok`
- Supabase：真实云端项目（Email Auth + RLS）
- AI：DeepSeek `deepseek-chat`，服务端 Key，仅经 FastAPI 调用
- 推送：PushPlus 真实通道

## 1. Supabase Auth/RLS（15/15 PASS）

- 创建两个真实 Auth 用户（admin API，email_confirm）并密码登录成功；错误密码被真实 Auth 拒绝（HTTP 400）
- 用户 A/B 各自经 RLS 写入 `schedule_state` 自己的行（HTTP 201）
- 用户 B 读取用户 A 的行被 RLS 拦截（0 行）；修改/删除用户 A 的行均被拦截（0 行受影响）
- 攻击后用户 A 的行仍存在且未被篡改
- 未登录 anon key 读取被 RLS 拦截（0 行），写入被拒绝（HTTP 401）
- service_role 可审计到两行数据（证明数据确实存在且 service role 旁路 RLS）
- 验收数据与测试用户已清理（remaining=0）

## 2. 真实 AI 调用（DeepSeek）

- `POST /api/v1/parse`（经 Next.js 生产代理）`provider=deepseek`
- 输入：`下周三下午三点去世纪公园跑步一小时`
- 响应：`source: "deepseek"`，解析结果 `2026-08-19 15:00-16:00`，category=fitness，location=世纪公园
- 说明：`source=deepseek` 且使用服务端 Key 调用真实 DeepSeek API，非本地回退

## 3. 微信推送端到端（PushPlus，8/8 PASS）

- 真实 Auth 用户经 RLS 写入带 `remindAt`（过去 2 分钟）的时间块（HTTP 201）
- `POST /api/v1/reminders/run`：`enabled=true, pushed=1, errors=[]`（真实 PushPlus 业务 code 200）
- 首次推送后 `reminder_log` 精确 1 条；再次扫描 `pushed=0` 且日志仍 1 条（去重生效）
- `reminders/status`：`enabled=true, channel=pushplus, scan_seconds=300`
- 环境内既有到期时间块（22/23 条）被去重跳过，未重复推送；既有用户 `reminder_log` 29 条未改动
- 验收行、日志与测试用户已清理（schedule_rows=0, log_rows=0）

## 说明

- 初版去重断言把环境中既有到期提醒的 `skipped` 计入总数导致误报；改为按 `block_id` 精确断言后 8/8 PASS，清理也按 client-side 过滤复核。
- 未记录任何 Key、完整邮箱、完整用户 ID；测试用户 ID 仅保留 8 位前缀。

- 证据位置：`.project-to-act/tasks/T-021/evidence/E-T021-001.md`
- 有效期：2026-08-22T20:47:58Z
