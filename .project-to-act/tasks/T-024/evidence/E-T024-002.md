# E-T024-002 Vercel Serverless 公网部署验收证据

- 任务：T-024 Vercel Serverless 形态改造
- 证据 ID：E-T024-002
- 时间：2026-08-16T19:20:00Z
- 验证方法：Vercel CLI 登录并创建两个项目 → 生产部署 → 公网 curl / Supabase REST / PushPlus / DeepSeek 端到端断言
- 验证结果：后端/前端健康检查、Supabase Auth/RLS、真实 DeepSeek、PushPlus 推送与 cron 鉴权全部 PASS；验收测试数据与测试用户已清理

## 公网地址

- 前端：`https://ai-schedule-web-ten.vercel.app`
- 后端：`https://ai-schedule-backend.vercel.app`
- Vercel 项目：`hhh-lm1/ai-schedule-backend`（Root Directory=`backend`）、`hhh-lm1/ai-schedule-web`（Next.js）

## 健康检查与代理链路

- `GET https://ai-schedule-backend.vercel.app/api/v1/health`：HTTP 200，`{"status":"ok","service":"ai-schedule-backend","version":"0.1.0"}`
- `GET https://ai-schedule-web-ten.vercel.app/api/health`：HTTP 200，`storage: "supabase"`（Supabase 环境变量在公网构建中生效）
- `GET https://ai-schedule-web-ten.vercel.app/api/v1/health`：HTTP 200，确认 Next.js rewrites 经公网代理到 FastAPI
- CORS 预检/响应：`Origin: https://ai-schedule-web-ten.vercel.app` 请求后端返回 `access-control-allow-origin: https://ai-schedule-web-ten.vercel.app`

## Supabase Auth/RLS（11/11 PASS）

通过真实 Supabase Admin Auth API 创建 A/B 两个测试用户并密码登录；跨用户隔离断言：

- admin 创建用户 A/B：HTTP 200
- A/B 密码登录：HTTP 200
- A 经 RLS 写入自己的 `schedule_state`：HTTP 201
- B 读取/修改/删除 A 的行：均被 RLS 拦截（0 行）
- 未登录 anon 读取 A 的行：0 行
- 未登录 anon 写入：HTTP >= 400 被拒绝
- service_role 审计：可读取到 A 的 1 行（数据真实存在且 RLS 旁路正常）

## 定时器鉴权与 PushPlus 推送（6/6 PASS）

- 无 `Authorization` 调用 `GET /api/v1/reminders/cron`：HTTP 401
- 错误 secret 调用：HTTP 401
- 正确 `Authorization: Bearer <CRON_SECRET>`：HTTP 200，扫描 `enabled=true, errors=[]`
- 测试时间块写入后，`reminder_log` 精确新增 1 条（真实 PushPlus 推送成功）
- 再次触发扫描：`reminder_log` 仍为 1 条（去重生效）
- `GET /api/v1/reminders/status`：`enabled=true, channel=pushplus, scan_seconds=300`

## 真实 AI 调用（DeepSeek PASS）

```bash
curl -X POST https://ai-schedule-web-ten.vercel.app/api/v1/parse \
  -H 'Content-Type: application/json' \
  -d '{"text":"下周三下午三点去世纪公园跑步一小时","today":"2026-08-17","provider":"deepseek"}'
```

响应：`source="deepseek"`，`2026-08-19 15:00-16:00`，`category=fitness`，`location=世纪公园`。真实 DeepSeek 服务端 Key 经公网 FastAPI 调用成功，非本地回退。

## 清理

- 测试用户 A/B 已通过 Admin API 删除（HTTP 200）
- 验证 cascade：`schedule_state` 与 `reminder_log` 残留均为 0 行

## 配置与已知限制

- 后端环境变量：`ENABLE_SCHEDULER=false`、`CRON_SECRET`（随机生成，存于 Vercel Encrypted）、`CORS_ORIGINS=https://ai-schedule-web-ten.vercel.app`、Supabase/DeepSeek/PushPlus 配置
- `backend/vercel.json` 已配置 Vercel Cron 每日 0 点（UTC）调用 `/api/v1/reminders/cron` 作为兜底
- GitHub Actions 5 分钟定时器示例位于 `deploy/github-actions/reminder-cron.yml`；当前 PAT 无 `workflow` scope，工作流文件未推入 `.github/workflows/`，配置 Secrets 后复制到该目录即可启用
- Hobby 版 Vercel Cron 每天最多 1 次，5 分钟级提醒仍需 GitHub Actions 或外部定时器

- 证据位置：`.project-to-act/tasks/T-024/evidence/E-T024-002.md`
- 有效期：2026-08-24T19:20:00Z
