# E-T019-001 PushPlus 业务错误码校验验证证据

- 任务：T-019 修复 PushPlus 业务错误码误判为推送成功（F-018 修订）
- 证据 ID：E-T019-001
- 时间：2026-08-11T04:00:00Z
- 验证方法：PushPlus 实测 + 后端单元测试
- 验证结果：
  - PushPlus 直测未实名：`{"code":905,"msg":"账户未进行实名认证"}`，HTTP 200 但业务失败
  - 后端修复后 `push_wechat_message` 对 905 返回 False；对 `code:200` 返回 True
  - `cd backend && .venv/bin/python -m pytest -q`：36 个测试全部通过，退出状态 0
  - 用户完成实名认证后 PushPlus 实测返回 `{"code":200,"msg":"执行成功"}`；`POST /api/v1/reminders/run` 返回 `pushed:3, errors:[]`，并真实写入 `reminder_log`

## 变更说明

- `backend/app/services/push.py`：PushPlus 通道不再仅看 HTTP 状态码，改为解析响应 JSON 并校验 `code == 200`
- `backend/tests/test_reminders.py`：新增 `test_pushplus_business_error_is_failure` 与 `test_pushplus_success_code`
- 清理了 3 条未实名认证期间产生的假成功 `reminder_log`，实名后重新推送成功

- 证据位置：`.project-to-act/tasks/T-019/evidence/E-T019-001.md`
- 有效期：2026-08-18T04:00:00Z
