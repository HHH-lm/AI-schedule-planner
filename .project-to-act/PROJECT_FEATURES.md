# 项目功能

> 功能范围与状态的唯一清单。功能变化后同步进度；未验证的功能不得标记为已完成。

## 状态定义

- 候选：尚未批准进入范围
- 已规划：已确认但未开始
- 进行中：正在实现
- 已阻塞：等待外部条件
- 已完成：完成条件已满足且有证据
- 已取消：明确退出范围并保留原因

## 功能清单

| 功能 ID | 功能 | 优先级 | 状态 | 依赖 | 完成条件 | 证据 ID |
|---|---|---|---|---|---|---|
| F-001 | 周计划视图（拖拽、完成打卡、地点、类目、点击空白新建） | 高 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 自动测试通过 + 手动验收 + 证据记录 | E-T004-003 |
| F-002 | 自然语言生成时间块（OpenAI / DeepSeek API 解析，未配置时回退本地中文 NLP，独立地点与带空格时间解析） | 高 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 评测样本通过 + 验收 + 证据记录 | E-T004-003 |
| F-003 | 宏观任务看板（14 天网格、子任务、双向联动） | 高 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 自动测试通过 + 手动验收 + 证据记录 | E-T004-003 |
| F-004 | 统计周报（类目统计、时间分布、完成率、Markdown 导出） | 中 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 自动测试通过 + 手动验收 + 证据记录 | E-T004-003 |
| F-005 | 本地存储与数据持久化 | 高 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 自动测试通过 + 验收 + 证据记录 | E-T004-003 |
| F-006 | ICS 导出（`src/lib/ics.ts`，README 未描述） | 低 | 已取消（暂缓） | 无 | 恢复导出功能后重新验收 | 无 |
| F-007 | Obsidian 关联与跳转（粘贴链接自动解析） | 中 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 自动测试通过 + 手动验收 + 证据记录 | E-T004-003 |
| F-008 | 撤销/重做历史栈（50 步、`⌘Z` / `⇧⌘Z` / `Ctrl+Y`、页面按钮） | 中 | 已完成（阶段 5 Gate 通过，人工验收待确认） | 无 | 自动测试通过 + 手动验收 + 证据记录 | E-T004-003 |
| F-009 | Supabase 云同步 Auth 多租户隔离（登录/注册、auth.uid()、RLS） | 高 | 已完成 | 无 | Auth 改造测试通过 + RLS SQL 就绪 | E-T011-001 |
| F-010 | 健康检查与运维手册（`/api/health`、部署/监控/回滚/备份） | 高 | 已完成 | 无 | 生产构建 + 本地探测通过，运行手册就绪 | E-T006-001 |
| F-011 | 可观测性（结构化日志、错误边界、关键事件） | 中 | 已完成 | 无 | 日志测试通过 + 生产构建通过 | E-T007-001 |
| F-012 | 安全评测（危险输入样本、Obsidian 协议、密钥扫描） | 高 | 已完成 | 无 | 安全样本测试通过 + 扫描全部 PASS | E-T008-001 |
| F-013 | dev 脚本防多实例（已有实例复用、异常清理重启） | 中 | 已完成 | 无 | 实例复用行为验证通过 + 证据入账 | E-T009-001 |
| F-014 | NLP 拒答（无意义输入拒绝 + QuickAdd 明确反馈） | 高 | 已完成 | 无 | 拒答评测通过 + 生产构建通过 | E-T010-001 |
| F-015 | AI API 解析服务（OpenAI / DeepSeek 双提供商、服务端 Key、设置页切换、本地回退） | 高 | 已完成 | F-002 | API 路由与双提供商测试通过 + 本地回退验证 | E-T012-001 |
| F-016 | FastAPI/Python 后端（AI 任务解析、任务拆解、时间规划、冲突检测、AI 调用全部迁移到后端） | 高 | 已完成 | F-015 | 后端 pytest 通过 + 前端代理联动验证 | E-T013-001 |
| F-017 | 今日待办视图与移动端/桌面双布局（今日安排、待办任务、明日预览、完成打卡、排到今天） | 高 | 已完成 | F-003 | 前端测试 + tsc + lint + build + 双端截图验证 | E-T014-001 |
| F-018 | 定时提醒与微信推送（APScheduler 每 5 分钟扫描、reminder_log 去重、企业微信/PushPlus/Server酱） | 高 | 已完成 | F-009, F-016 | 后端 pytest 34 个测试通过 + 提醒端点探测通过 | E-T015-001 |
| F-020 | 记忆系统（用户主动管理长期偏好、习惯、生活/工作偏好、长期约束，AI 只生成候选记忆） | 中 | 已完成 | 无 | 前端测试 + tsc + lint + build + 后端 pytest 通过 | E-T020-001 |
| F-019 | 今日待办四象限（紧急/重要矩阵分组、新建任务象限选择、任务看板象限色点） | 高 | 已完成 | F-017 | 前端测试 + tsc + lint + build + 注入数据截图验证 | E-T016-001 |
| F-021 | AI 解析质量评测与回归（30 条 golden set：10 QuickAdd + 10 Planning + 5 边界异常 + 5 Constraint/Memory） | 高 | 已完成 | F-015 | 评测命令通过 + 真实 DeepSeek 30/30 + 证据入账 | E-T022-001 |
| F-022 | 跨天时间块支持（手动结束日期、自然语言跨天解析、周计划分段渲染、冲突/统计/提醒跨天） | 高 | 进行中 | F-001, F-002 | 自动测试通过 + 人工验收跨天场景 + 证据入账 | E-F022-001 |

## 功能变更历史

按时间倒序追加：日期、功能 ID、变化、原因、影响、证据 ID 和确认来源。

- 2026-08-18：F-018，修订提醒过期行为：后端扫描只处理最近 10 分钟内的到期 `remindAt`，提醒时间已过去超过 10 分钟（含服务恢复后积压）不再补发；原因：用户实测时间块已过去仍收到微信提醒；影响：`backend/app/services/reminders.py`、提醒相关测试、README；证据 E-T029-001，来源：用户反馈定位。
- 2026-08-17：F-022，新增跨天时间块支持：TimeBlock.end 允许超过 1440 表示跨到次日或更晚；BlockModal 增加“结束日期”，结束时间早于开始时间按次日处理；自然语言解析支持“今晚10点到明天早上8点”“周五晚10点到周六早上8点”等表达；周计划按天切段渲染并支持拖拽/调整；冲突检测、槽位查找、今日待办、任务看板、统计周报、微信提醒与记忆分析同步跨天语义；golden set 扩至 33 条并纳入跨天/1440 边界；原因：用户反馈周计划不支持跨天任务；影响：前端时间轴/弹窗/各视图与后端 NLP/AI/conflict/slot_finder/reminders/memory_analysis/validator；证据 E-F022-001，来源：用户指令。
- 2026-08-17：F-020，补录证据索引：既有证据 E-T020-001 已存在但功能清单标记“待补充”，T-026 账本一致性修复后改为 E-T020-001；原因：验收索引与证据文件不一致；影响：仅文档索引，无业务逻辑变化；证据 E-T026-001，来源：用户指令。
- 2026-08-16：F-021，新增 AI 解析质量评测：30 条中文 golden set（10 QuickAdd + 10 Planning + 5 边界异常 + 5 Constraint/Memory），新增 `backend/app/golden_ai_cases.py` 与 `backend/app/eval_ai_golden.py`；评测发现并修复 weekday_label 周日错标、晚上偏好按时段中点误判、记忆未应用到全部任务；真实 DeepSeek 四类均 100%，证据 E-T022-001，来源：用户指令。
- 2026-08-11：F-018，修订 PushPlus 推送判定：PushPlus 通道从“仅看 HTTP 状态码”改为解析响应并校验 `code == 200`，`code 905`（未实名认证）等业务失败不再误记成功、不写 `reminder_log`、可自动重试；原因：用户实测 `pushed:3` 但微信收不到，直测 PushPlus 返回 905；影响：`push_wechat_message`、提醒去重与重试语义；证据 E-T019-001，来源：用户反馈定位。
- 2026-08-11：F-018，修订 remindAt 保存链路：修复 BlockModal 保存时遗漏 `remindAt` 导致云端时间块无提醒时间的缺陷；`addParsedBlocks`、`planTasks` 生成的新块和待排期拖拽落点自动写入“开始前 5 分钟”默认提醒；原因：用户实测 `pushed:0`，云端 32 个时间块全部缺 `remindAt`；影响：`saveBlock`、WeekTimeline 落点、`defaultRemindAtISO` 工具；证据 E-T018-001，来源：用户反馈定位。
- 2026-08-11：F-018，修订默认提醒行为：BlockModal 新建/编辑时间块时，若未设置过提醒，自动预填“开始前 5 分钟”；修改日期或开始时间且未手动触碰提醒时自动重算，跨天正确回落到前一天；手动修改后保留手动值，清空则关闭提醒；原因：用户反馈每次手动填写微信提醒时间冗余；影响：BlockModal 默认值逻辑、`remindBeforeInput` 工具、提醒输入提示文案；证据 E-T017-001，来源：用户反馈。
- 2026-08-11：F-019，新增今日待办四象限：任务增加 `priority`（紧急且重要 / 重要但不紧急 / 紧急但不重要 / 既不紧急也不重要），新建/编辑任务可选择象限，今日待办按矩阵分组（移动端单列、桌面端 2x2），任务看板显示象限色点；原因：用户希望所有待办按紧急/重要矩阵组织，并在建任务时选择类型；影响：`TaskQuadrant` 类型、`priorities.ts` 元数据、TaskModal 象限选择器、TodayView/TaskBoard 展示；证据 E-T016-001，来源：用户指令。
- 2026-08-11：F-018，新增定时提醒：时间块可设置提醒时间，FastAPI 后端 APScheduler 按 `REMINDER_SCAN_SECONDS`（默认 300 秒）扫描 Supabase `schedule_state` 中到期待提醒的时间块并推送微信；推送成功后写入 `reminder_log` 去重，失败自动重试；原因：用户希望手机锁屏也能收到提醒，不受页面是否打开影响；影响：`TimeBlock.remindAt`、BlockModal 提醒输入、后端 `/api/v1/reminders/{status,run}`、Supabase `reminder_log` 表；证据 E-T015-001，来源：用户指令。
- 2026-08-11：F-017，新增今日待办视图：应用默认打开“今日待办”，移动端（<768px）单列卡片流、桌面端（>=768px）双栏仪表盘；原因：用户希望手机端能方便查看待办，且桌面端呈现形式应完全不同；影响：`ViewMode` 增加 `today`，导航新增“今日”入口，今日安排/待办任务/明日预览支持打卡、编辑、Obsidian 跳转与“排到今天”；证据 E-T014-001，来源：用户指令。
- 2026-08-10：F-015，AI 解析超时与失败反馈优化：请求默认 15 秒超时（`AI_TIMEOUT_MS` 可配置），`max_tokens=1000` 限制输出，提示词精简；超时或失败时 QuickAdd 显示明确错误并保留输入，不再静默回退本地规则；证据 E-T012-002，来源：用户反馈（长句解析长时间无响应、失败提示不明显）。
- 2026-08-10：F-016，新增 FastAPI/Python 后端并迁移核心逻辑：自然语言解析、任务拆解、时间规划、冲突检测、AI 调用全部由 `backend/` 实现，前端通过 Next.js rewrites 调用 `/api/v1/*`，本地 NLP/AI/冲突检测前端实现移除；证据 E-T013-001，来源：用户指令（加入 FastAPI 后端与 Python 技术栈）。
- 2026-08-10：F-015，新增 AI API 解析服务：`POST /api/parse` 支持 OpenAI / DeepSeek，设置页可选解析服务，未配置 Key 或接口失败自动回退本地 NLP 规则；证据 E-T012-001，来源：用户指令。
- 2026-08-10：F-009，从显式 user_id 单用户边界升级为登录式多租户：AuthModal 登录/注册/登出，user_id 取 auth.uid()，schema.sql 默认启用 RLS；证据 E-T011-001，来源：用户确认升级（每人登录、每人一库）。
- 2026-08-10：F-014，规则 NLP 增加拒答：纯符号、无效星期、缺少事项名等输入不再生成默认块，QuickAdd 显示具体原因；证据 E-T010-001，来源：用户 P2 清单。
- 2026-08-10：F-013，将既有 dev 脚本防多实例变更补入任务与证据，并刷新关键文件哈希；证据 E-T009-001，来源：用户 P1 清单 + agentops 健康检查。
- 2026-08-10：F-012，新增 `security.test.ts`、`scripts/scan-secrets.sh` 与 `docs/security.md`，形成可重复执行的安全基线；证据 E-T008-001，来源：用户 P1 清单。
- 2026-08-10：F-011，新增 JSON 日志工具、AppErrorBoundary 与初始化/NLP/Supabase 关键事件日志；证据 E-T007-001，来源：用户 P1 清单。
- 2026-08-10：F-010，新增健康检查端点与部署运维运行手册，覆盖检查清单、部署流程、监控、回滚、备份与事故处理；证据 E-T006-001，来源：用户 P0 清单。
- 2026-08-10：F-009，云同步从全局 singleton 行改为 user_id 隔离，未配置用户 ID 时禁用；多人部署需独立 user_id 或接入 Auth + RLS；证据 E-T005-001，来源：用户 P0 清单。
- 2026-08-09：F-001/F-002/F-006/F-007/F-008，阶段 5 功能迭代：独立地点片段合并、带空格时间解析、待排期拖拽、点击空白新建、Obsidian 关联与跳转、撤销/重做；ICS 导出暂缓置灰；证据 E-T004-001，来源：用户反馈与自动验证。
- 2026-08-09：F-001/F-002，自然语言生成时间块后自动切换周并滚动聚焦新块区域，短暂高亮；影响周时间轴生成体验，证据 E-T003-001，来源：用户反馈与自动验证。
- 2026-08-09：F-002，自然语言周几解析从“绑定当前展示周”改为“从当前时间起算下一个指定星期几”，避免本周已过去的日期；影响周时间轴快速输入，证据 E-T002-001，来源：用户反馈与自动验证。
- 2026-08-08：F-001 至 F-006，根据 `src/` 与 README 建立初始功能清单，状态为代码已存在但未验收；来源为 T-ADOPT-001 采用记录，证据 E-ADOPT-001。
- 暂无记录。
