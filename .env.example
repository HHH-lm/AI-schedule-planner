# Supabase 配置（可选，不填则使用本地存储模式）
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 云同步用户 ID（必填，多人部署时每实例配置独立 ID，避免数据串用）
NEXT_PUBLIC_SUPABASE_USER_ID=

# OpenAI API Key（可选，用于增强 AI 自然语言解析，不填使用本地规则）
OPENAI_API_KEY=
